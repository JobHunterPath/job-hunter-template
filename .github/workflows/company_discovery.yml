# This workflow has been removed. It will be deleted from your repo automatically
# on the next successful template sync. Safe to delete this file manually.
name: (Removed) Company Discovery
on:
  workflow_dispatch:
jobs:
  removed:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Company discovery has been removed. This file will be cleaned up on next template sync."

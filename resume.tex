% Default resume entrypoint.
% The template repo uses the double-column version by default.
% To use the single-column version, set config/api_config.yml:
% profile.resume_tex: "resume_single_column.tex"

\input{resume_double_column.tex}
